[project]
name = "chip8"
version = "0.1.0"
description = ""
authors = [
    {name = "Samuel Jones",email = "sjones5516@floridapoly.edu"}
]
readme = "README.md"
license = "MIT"
requires-python = ">=3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "chip8", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)"
]
